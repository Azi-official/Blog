@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap');

/* ---------- Base ---------- */
:root{
  --bg: #0a0a0a;
  --card: #111;
  --red: #e50914;
  --red-dark: #b0060f;
  --muted: #bfbfbf;
}

*{box-sizing:border-box}
body{
  margin:0;
  min-height:100vh;
  background:var(--bg);
  color:#fff;
  font-family:'Poppins',sans-serif;
  display:flex;
  flex-direction:column;
  align-items:center;
  padding:36px 20px;
}

/* ---------- Page title ---------- */
.page-title, .article-header .article-title{
  color:var(--red);
  text-shadow:0 0 10px rgba(229,9,20,0.45);
  font-weight:700;
  letter-spacing:0.5px;
  margin:0 0 22px;
  text-align:center;
}

/* ---------- Blog card (index.html) ---------- */
.blog-card{
  width:360px;
  background:var(--card);
  border-radius:14px;
  border:2px solid var(--red);
  box-shadow:0 0 22px rgba(229,9,20,0.25);
  overflow:hidden;
  transition:transform .25s ease, box-shadow .25s ease;
}
.blog-card:hover{
  transform:translateY(-8px);
  box-shadow:0 0 40px rgba(229,9,20,0.45);
}

.featured-image{
  display:block;
  width:100%;
  height:220px;
  object-fit:cover;
  border-bottom:2px solid var(--red);
}

.blog-content{
  padding:20px;
  text-align:center;
}
.blog-title{
  font-size:1.25rem;
  margin:8px 0 6px;
  color:#fff;
}
.author-name{ color:var(--muted); margin:0; font-size:0.95rem }
.publication-date{ color:var(--red); margin:8px 0; font-size:0.9rem }
.excerpt{ color:#d0d0d0; font-size:0.95rem; line-height:1.45; margin-bottom:18px }

/* READ ARTICLE button (styled as small button) */
.read-btn{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:10px;
  background:var(--red);
  color:#fff;
  text-decoration:none;
  padding:9px 18px;
  border-radius:8px;
  font-weight:600;
  font-size:15px;
  transition:background .2s ease, transform .15s ease;
  box-shadow:0 6px 14px rgba(229,9,20,0.18);
}
.read-btn:hover{ background:var(--red-dark); transform:translateY(-3px) }

/* ---------- Article page ---------- */
.article-page{
  width:100%;
  max-width:920px;
  padding-bottom:60px;
}

.article-header{
  display:flex;
  flex-direction:column;
  align-items:center;
  gap:8px;
  margin-bottom:20px;
}
.article-header .article-sub{
  color:var(--muted);
  margin:0;
  font-size:0.95rem;
}

/* Back link */
.back-link{
  color:var(--muted);
  text-decoration:none;
  align-self:flex-start;
  margin-left:20px;
  margin-bottom:10px;
  display:inline-flex;
  gap:8px;
  align-items:center;
}
.back-link i{ color:var(--red) }

/* Article content layout */
.article-main{
  background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
  border-radius:12px;
  padding:18px;
  border:2px solid rgba(229,9,20,0.12);
  box-shadow:0 0 24px rgba(229,9,20,0.08);
  width:90%;
  max-width:800px;
  margin:0 auto;
}

.article-figure img{
  width:100%;
  border-radius:10px;
  display:block;
  margin-bottom:18px;
  object-fit:cover;
  height:360px;
  border:2px solid var(--red);
}

.article-content{
  color:#ddd;
  font-size:1rem;
  line-height:1.65;
}

.article-content p{ margin-bottom:14px }

/* CTA subscribe button */
.subscribe-btn{
  display:inline-flex;
  align-items:center;
  gap:8px;
  text-decoration:none;
  background:var(--red);
  color:#fff;
  padding:10px 16px;
  border-radius:8px;
  font-weight:600;
}
.subscribe-btn:hover{ background:var(--red-dark) }

/* Footer */
.article-footer{ color:var(--muted); text-align:center; margin-top:20px; font-size:0.9rem }

/* ---------- Responsive ---------- */
@media (max-width:420px){
  .blog-card{ width:320px }
  .featured-image{ height:200px }
  .article-figure img{ height:220px }
  .back-link{ margin-left:12px }
}
